#include <iostream>
#define endl '\n'
using namespace std;

int fac(int N){
	if(N==0) return 1;
	else return N*fac(N-1);
}

int main(){
    ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	int N;
    cin >> N;
	cout << fac(N) << endl;
	return 0;
}